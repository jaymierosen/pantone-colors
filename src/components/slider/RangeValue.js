import React from "react";

const RangeValue = ({ currYear }) => {
  return (
    <section className="range-slider__value">
      <h4>{currYear}</h4>
    </section>
  );
};

export default RangeValue;
